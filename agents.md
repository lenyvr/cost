# Agent Status and Work Plan (Logbook)

*IInstructions for the Agent: Keep this file updated. Every time you implement a use case, a business rule, or make a
technical decision, record it here. Mark completed tasks with [x].*

## 1. Actual project status
- **Current phase:** Design and implementation of infrastructure.
- **Estructura base:** Already created (packages `domain`, `application`, `infrastructure` configured with Gradle
  Kotlin DSL and Docker).

### 1.1 Entity descriptions
- **measurement_unit**:
    - attributes:
        - code: String primary key, must be sent in the request.
        - name: must be unique,  must be sent in the request.
        - active: Indicate the status of the record, if it's 'true' is active, if it's 'false' is inactivated.
        - created_date: date managed by de database.
- **product_type**:
    - attributes:
        - id: primary key, autogenerated by the database.
        - name: must be unique,  must be sent in the request.
        - active: Indicate the status of the record, if it's 'true' is active, if it's 'false' is inactivated.
        - created_date: date managed by de database.
- **product_currency**:
    - attributes:
        - id: primary key Integer, autogenerated by the database.
        - name: must be unique,  must be sent in the request.
        - symbol: must be sent in the request.
        - code: unique, must be sent in the request. 
        - description: Optional, could be sent in the request.
        - active: Indicate the status of the record, if it's 'true' is active, if it's 'false' is inactivated.
        - created_date: date managed by de database.
- **product**:
    - attributes:
        - id: primary key Integer, autogenerated by the database.
        - name: Unique product name, must be sent in the request.
        - amount_value: Double that must be sent in the request. 
        - measurement_unit_code: the String foreign key to the measurement_unit table, received in the request.
        - product_currency_id: the integer foreign key to the product_currency table, received in the request.
        - product_type_id: the integer foreign key to the product_type table, received in the request.
        - active: Indicate the status of the record, if it's 'true' is active, if it's 'false' is inactivated.
        - created_date: date managed by de database.

### 1.2 Entity values, parameters already register in tables
- **measurement_unit**: columns code,name.
    - values:'GR','Gramo'; 'L','Litro'; 'M', 'Metro' and 'UN','Unidad'.
- **product_type**: column name.
    - values: 'Ingrediente'; 'paqueteria' and 'Servicio'.
- **product_currency**: columns name, symbol, code, description
    - values: 'Pesos';'$';'COP' and 'Pesos Colombianos'
  
## 2. Roadmap and Upcoming Use Cases
*Agent: Break down here the technical steps (ports, use cases, adapters, beans) for the requirements that I will request.
Always strictly follow Hexagonal Architecture principles (Domain, Application, Infrastructure).*

**Tasks to be done:**

- [ ] Create measurement unit: 
    - Follow Hexagonal layers. Remember business rules. Remember create DTOS for request and response. 

## 3. Technical Decisions Made
*Record here any significant changes to the code, custom exceptions, mappers, or design patterns used.*

## 4. Notes and Bugs
*Note here if business information is missing, if there is a bug in Docker, or if there are any pending dependencies.*